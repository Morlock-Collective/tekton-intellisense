apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: build-and-deploy-2
spec:
  tasks:
    - name: build
      taskRef:
        name: build-image
    - name: deploy
      taskRef:
        name: deploy-app
      params:
        - name: image-digest
          value: $(tasks.build.results.digest)
