apiVersion: triggers.tekton.dev/v1beta1
kind: EventListener
metadata:
  name: main-listener
spec:
  serviceAccountName: tekton-triggers-sa
  triggers:
    - name: delegated
      triggerRef: build-trigger
